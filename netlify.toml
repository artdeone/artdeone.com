[build]
  functions = "netlify/functions"
 
  publish = "/"          # သို့မဟုတ် "/" လို့ ပြန်ပြောင်းပါ (Root is Root)

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
